/**
 * OBSOLETO — Script de migração legado.
 *
 * As colunas `subscription_status` e `subscription_expires_at` foram removidas
 * da tabela `stores` no redesign de 2026-06-01.
 * A assinatura agora vive na tabela `subscriptions` (Fase 0 do novo schema).
 *
 * Este arquivo é mantido apenas para referência histórica.
 */
export {}
